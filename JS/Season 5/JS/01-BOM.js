"use strict"

//BOM blowser object model


console.log(window.innerHeight) //te da la altura del navegador
console.log(window.innerWidth) //te da la anchura+
console.log(window.location) //te da el URL que estamos

function abrir(url) {
    window.open(url);
}